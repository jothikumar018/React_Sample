import React from 'react';
import { Typography } from "@mui/material";

const User = () => {
    return (
        <React.Fragment>
            <h1>User</h1>
            <Typography paragraph>
               test
            </Typography>
        </React.Fragment>
    );
};

export default User;